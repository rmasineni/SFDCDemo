trigger trg_Async_Process_Queue on Async_Process_Queue__c (before insert, before update, after insert, after update) {
 /*
    Boolean skipValidations = false;
    skipValidations = SkipTriggerValidation.performTriggerValidations();
    
    if(skipValidations == false){ 
       SfLead.leadDeDupService.handleAsyncProcessQueueTrigger(); 
    }*/
}