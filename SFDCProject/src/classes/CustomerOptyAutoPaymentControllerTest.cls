@IsTest
public class CustomerOptyAutoPaymentControllerTest {
    testmethod public static void testController() {
    
        //There is nothing to test in this controller but otherwise, it will show the class coverage as 0% so this basic test code.
        new CustomerOptyAutoPaymentController();
    }
}