task parseXml() {
    def parsedProjectXml = (new XmlParser()).parse('ant/package.xml')
    println parsedProjectXml
    println parsedProjectXml.@version
    println "TypesList name : " + parsedProjectXml.types[0].@value +
            ", members : " + parsedProjectXml.types[0].@value
    parsedProjectXml.types.members.each{ module ->
      println "Name : " + members.@value
    }
}
